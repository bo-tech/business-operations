Tryton in production runs `v0.19.0`, which carries the party modules a
contact pool needs and the `bv_crm` module on top of them. The
accounting databases gain nothing: none of it is activated there, and
the pool runs in a database of its own
