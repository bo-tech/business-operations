The microVM documentation covers hosts that cannot put guests on the
LAN, such as a workstation whose only interface is wifi. Those give the
bridge no uplink and masquerade the guest traffic instead.
