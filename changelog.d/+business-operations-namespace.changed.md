The platform options are named `business-operations.*` rather than
`custom.business-operations.*`, so that the flake's options carry the
project's name instead of a generic prefix that identifies no owner.
