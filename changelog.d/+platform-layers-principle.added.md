An architecture principle names the platform's three layers — a core
platform a consumer supplies settings for, components it switches on or
off, and an application catalog it selects from — so a consumer knows
which interface it is meeting.
