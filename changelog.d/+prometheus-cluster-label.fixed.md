Prometheus labels its metrics with the cluster it runs on rather than
with the name of one particular cluster. The platform named `cluster-0`
outright, so any other site adopting it would have reported that name as
its own.
