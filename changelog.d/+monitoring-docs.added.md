A page describes the monitoring stack: where Prometheus, Alertmanager
and Grafana run, where their values and alert rules sit, that no
receiver is configured so alerts are never delivered, and that node
metrics and logs are not collected.
