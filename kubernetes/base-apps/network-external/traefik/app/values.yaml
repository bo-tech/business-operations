providers:
  kubernetesCRD:
    enabled: false
  kubernetesIngress:
    enabled: false
  kubernetesGateway:
    enabled: true
    labelSelector: local/gateway-role=external

gatewayClass:
  enabled: true
  name: external
  labels:
    local/gateway-role: external

gateway:
  enabled: true
  name: external
  listeners:
    web:
      port: 8000
      protocol: HTTP
      namespacePolicy:
        from: All

ingressClass:
  enabled: false

ingressRoute:
  dashboard:
    enabled: false

service:
  type: LoadBalancer
  annotations:
    lbipam.cilium.io/ips: "${cluster_external_ip}"
  labels:
    ip-range: static-pool

ports:
  web:
    port: 8000
    expose:
      default: true
    exposedPort: 80
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true
  websecure:
    port: 8443
    expose:
      default: true
    exposedPort: 443

logs:
  general:
    level: INFO
  access:
    enabled: true
    format: json

resources:
  requests:
    cpu: 10m
    memory: 50Mi
