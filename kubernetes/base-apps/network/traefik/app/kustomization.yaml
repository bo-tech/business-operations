---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: network
resources:
  - ./helmrelease.yaml
  - ./middleware-forwardauth.yaml
  - ./gateway-git-pages.yaml

configMapGenerator:
  - name: traefik-internal-values
    files:
      - values.yaml=./values.yaml

configurations:
  - name-reference.yaml
