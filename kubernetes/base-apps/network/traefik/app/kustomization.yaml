---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: network
resources:
  - ./helmrelease.yaml
  - ./gateway-git-pages.yaml
  - ../../../../shared/authelia-forwardauth

configMapGenerator:
  - name: traefik-internal-values
    files:
      - values.yaml=./values.yaml

configurations:
  - name-reference.yaml
