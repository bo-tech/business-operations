apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitlab-registry-notification
  namespace: gitlab
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: gitlab-registry-notification
    creationPolicy: Owner
    template:
      type: Opaque
  data:
    - secretKey: secret
      remoteRef:
        key: apps/gitlab/registry-notification
        property: secret
