apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitlab-redis-secret
  namespace: gitlab
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: gitlab-redis-secret
    creationPolicy: Owner
    template:
      type: Opaque
  data:
    - secretKey: secret
      remoteRef:
        key: apps/gitlab/redis-secret
        property: secret
