apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitlab-gitlab-workhorse-secret
  namespace: gitlab
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: gitlab-gitlab-workhorse-secret
    creationPolicy: Owner
    template:
      type: Opaque
  data:
    - secretKey: shared_secret
      remoteRef:
        key: apps/gitlab/gitlab-workhorse-secret
        property: shared_secret
