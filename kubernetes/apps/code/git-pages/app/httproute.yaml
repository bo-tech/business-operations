---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: git-pages
  namespace: code
spec:
  parentRefs:
    - name: internal
      namespace: network
  hostnames:
    - "*.pages.${cluster_domain}"
  rules:
    - backendRefs:
        - name: git-pages
          port: 3000
