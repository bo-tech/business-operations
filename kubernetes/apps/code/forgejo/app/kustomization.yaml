---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: code
resources:
  - ./helmrelease.yaml

configMapGenerator:
  - name: forgejo-helm-values
    files:
      - values.yaml

configurations:
  - name-reference.yaml
