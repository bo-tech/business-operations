---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gitlab
resources:
  - cronjob.yaml
  - pvc-renovate-cache.yaml

configMapGenerator:
  - name: renovate-config
    files:
      - config.json
