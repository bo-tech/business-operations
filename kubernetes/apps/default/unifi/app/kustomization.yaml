---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ./helmrelease.yaml
  - ./httproute.yaml

configMapGenerator:
  - name: unifi-helm-values
    files:
      - values.yaml

configurations:
  - name-reference.yaml
