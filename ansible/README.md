# Ansible Roles and Playbooks

Ansible-based automation for k0s cluster deployment. Automates `nixos-anywhere`
calls and the subsequent steps to bootstrap the cluster.

Roles live in `roles/`, playbooks in `playbooks/`.

## See also

- [Ansible chapter](../docs/kubernetes/ansible.rst) — the devShell, the
  playbooks and what each one does, the inventory variables, and the known
  limitations
